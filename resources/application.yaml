security:
  disabled: true
  jwt:
    key: super-secret
  login:
    admin:
      pwd: admin

db:
  url: "sqlite:///database.db"

storage:
  studies: examples/studies/

launcher:
  default: local
  local:
    binaries:
      700: path/to/700

debug: true
